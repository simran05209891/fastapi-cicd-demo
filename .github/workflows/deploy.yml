name: Deploy FastAPI

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Server
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: 20.197.47.46
          username: whizcamp
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/whizcamp/project
            python3 deploy.py
